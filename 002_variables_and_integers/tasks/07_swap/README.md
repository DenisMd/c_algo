# Перемена местами значений переменных

С клавиатуры вводится 2 целых числа a и b. Поменяйте значений переменных т.е. чтобы в a было b и в b было a

Пример ввода:

```
12 7
```
Пример вывода:

```
a = 7 b = 12
```