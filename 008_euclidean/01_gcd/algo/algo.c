#include "algo.h"
#include <assert.h>
#include <limits.h>

long long gcd(long long a, long long b)
{
  assert(a != LLONG_MIN);
  assert(b != LLONG_MIN);
  // TODO:
  return 0;
}

long long gcd_rec(long long a, long long b)
{
  assert(a != LLONG_MIN);
  assert(b != LLONG_MIN);
  // TODO:
  return 0;
}
