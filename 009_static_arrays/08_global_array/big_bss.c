// Большой НЕинициализированный глобальный массив -> секция .bss.
// Хранить нечего (одни нули), в файле лежит только запись "нужно 1 МБ нулей".
// Поэтому бинарник почти не вырастет, хотя массив такого же размера, как в
// big_data.c. Сравни размеры файлов: ls -l big_data big_bss.

#define N (256 * 1024) // 256K int = 1 МБ памяти... но НЕ в файле

int big[N]; // без инициализатора -> .bss

int main(void) {
  return big[0];
}
